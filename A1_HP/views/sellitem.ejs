<!DOCTYPE html>
<html lang="en">
<head>
    <title>Sell Item</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container">
    <h1 class="mt-3">Sell Item</h1>

    <div class="card mb-4">
        <div class="card-body">
            <h5 class="card-title"><%= item.name %></h5>
            <p class="card-text"><strong>Available Stock:</strong> <%= item.quantity %></p>
            <p class="card-text"><strong>Current Price:</strong> $<%= item.price.toFixed(2) %></p>
        </div>
    </div>

    <form action="/sell" method="POST">
        <input type="hidden" name="itemId" value="<%= item.id %>">
        <input type="hidden" name="price" value="<%= item.price %>">
        
        <div class="form-group">
            <label for="quantity"><strong>Quantity to Sell</strong></label>
            <input type="number" 
                   class="form-control" 
                   id="quantity" 
                   name="quantity" 
                   min="1" 
                   max="<%= item.quantity %>" 
                   required>
            <small class="form-text text-muted">Maximum available: <%= item.quantity %></small>
        </div>
        
        <div class="mt-3">
            <button type="submit" class="btn btn-primary">Complete Sale</button>
            <a href="/" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</body>
</html>